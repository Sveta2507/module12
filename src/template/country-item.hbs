{{#each this}}
<li>
    <h1>
        {{name}}
    </h1>
    <img src="{{flag}}" alt="{{name}}" width="350px">
    <ul>
        {{#each languages}}
        <li>
            {{this.}}
        </li>
        {{/each}}
    </ul>
</li>
{{/each}}